Archived files (moved on user request)

These files were moved from the main tree into `archive/removed_tools/` to reduce
lint/noise and keep the runtime codebase minimal. They are preserved here so you
can restore them if needed.

- moved from `tools/` and top-level `recovered_main_from_pyc.py`

If you want them permanently removed, delete this folder after verification.
